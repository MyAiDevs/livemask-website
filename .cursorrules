# LiveMask Website - AI Development Rules v3.7

## Always Start Here (Mandatory Pre-check)
@../livemask-docs/ai-rules/v3.7/00-Core-Principles.md
@../livemask-docs/ai-rules/v3.7/04-Multi-Repo-Linkage.md
@../livemask-docs/ai-rules/v3.7/13-Multi-Repo-Development.md
@../livemask-docs/ai-rules/v3.7/16-Task-Completion-Report.md
@../livemask-docs/docs/development/LiveMask_开发任务清单与里程碑_v3.6.md

## Mandatory Task / Repository Match Gate
Before writing any file, confirm the current TASK explicitly includes
`livemask-website` as primary/affected repository or this Website window is
named in the Cursor handoff. If it does not match, stop and report:

```text
BLOCKED: current repository does not match TASK scope.
Current repo: livemask-website
Task: <TASK-ID>
Expected repos: <list from TASK>
No files changed.
```

## Website Specific Focus
Marketing pages, SEO, onboarding landing pages.
Ensure consistency with App download flow and Backend referral/affiliate logic.

## API Client Rule
Business API files must not call `fetch()` directly.

Use the shared Website HTTP client instead:
- Use `publicFetch` for public endpoints such as Blog, sitemap, RSS, marketing,
  and SEO crawler-safe content.
- Use `authFetch` for protected user endpoints such as account, nodes, billing,
  devices, subscription, checkout, and any logged-in portal data.
- Only `auth-api.ts` and the shared HTTP client file may call raw `fetch()` for
  auth bootstrap, refresh, logout, public transport, or low-level request
  handling.

All protected Website calls must automatically attach
`Authorization: Bearer <access_token>`, retry once after a successful refresh on
401, and must never log tokens or place tokens in URLs/query strings. Public
Blog/SEO calls must not depend on login state or require Authorization.

## Multi-Window Development
This window is for **Website / Marketing**.
You may use `gh` commands to operate GitHub API, Issues, Actions, `workflow_dispatch`, and `repository_dispatch`.
Always start from `dev`. If local `dev` does not exist, create it from `origin/dev` or `origin/main`.
Startup command:
```bash
git fetch origin main dev
git checkout dev
git pull --ff-only origin dev
```
Never implement daily development directly on `main`; `main` is staging/pre-release only.
Production work must come from a GitHub Release / `v*` tag, not from an ad hoc branch.
`task-unlocked` is only a development coordination signal; never use it to trigger `livemask-ci-cd`.
Coordinate landing page changes with App Onboarding and Backend referral systems.

## Mandatory Task Finish / Dev Merge Gate
A task is not complete while its changes exist only on `task/*`, `codex/*`, or another feature branch.

After implementation and required validation pass:
1. Commit the task changes with the `TASK-XXXX` ID.
2. Switch to `dev` and update from `origin/dev`.
3. Merge the completed task branch into `dev`.
4. Resolve conflicts inside the current repository boundary only.
5. Re-run Website validation on `dev` (`npm run build`, TypeScript checks, tests if present, and SEO generation checks when relevant).
6. Push `dev` to `origin/dev`.
7. Include task branch commit, dev merge commit, pushed remote ref, and validation evidence in the completion report.

Do not report `completed` if the task branch has not been merged into `dev`, if `dev` has not been pushed to `origin/dev`, if validation only passed before the dev merge, or if merge conflicts remain unresolved.

CI/CD smoke and staging validation must run from `dev` only. Do not use `task/*`, `codex/*`, or feature branch smoke as final evidence for Website completion; it is only a precheck until merged and pushed to `origin/dev`.

## Task Completion Report
When a task or subtask is complete, use the standard completion report from:
@../livemask-docs/ai-rules/v3.7/16-Task-Completion-Report.md

The report must explicitly say which other repo windows are unlocked for parallel development and which remain blocked.

After a meaningful completion, run task sync from `livemask-docs` with `gh workflow run task-sync.yml --repo MyAiDevs/livemask-docs --ref dev`.
